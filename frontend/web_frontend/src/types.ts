export type Post = {
    _id: string
    username: string
    location: { latitude: number; longitude: number }
    photo: string
    description: string
    animal: string
    postedDate: string
  }
  